# Project Instructions

- Work only inside `/Volumes/code/domyshev/sport-app` for this project.
- Do not modify `from_garmin_official_export` unless the user explicitly asks to change export files.
- For every user request in this project, append the request text to `docs/steps_human.md` as the next numbered step before completing the work, unless the user explicitly says not to update the steps.
- Use the existing `docs/steps_human.md` format for new steps: a fixed-width 16x16 `text` block with `шаг N` inside the symbolic picture, followed by a separate `text` block containing the request.
- Keep user-facing project documentation in Russian when the user writes in Russian.
